# API base URL for the FastAPI backend
#   Leave empty to use the Vite dev proxy (same machine, port 5173)
#   Set to full URL when backend is on a different machine
#   e.g. VITE_API_URL=http://192.168.1.50:8000/api
VITE_API_URL=
